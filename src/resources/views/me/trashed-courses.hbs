<div class="mt-4">
  <div>
    <h2>Khóa Học Của Tôi</h2>
    <a href="/me/stored/courses">Danh sách khóa học</a>
  </div>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Tên Khóa Học</th>
        <th scope="col">Trình độ</th>
        <th scope="col">Thời Gian Tạo</th>
      </tr>
    </thead>
    <tbody>
      {{#each course}}
        <tr>
          <th scope="row">{{sum @index 1}}</th>
          <td>{{this.name}}</td>
          <td>{{this.level}}</td>
          <td>{{this.createdAt}}</td>
          <td>
            <a
              href="/courses/{{this._id}}/restore"
              class="btn btn-link btn-restore"
              data-id="{{this._id}}"
            >Khôi Phục</a>
            {{! đường dẫn chuẩn trong url thêm localhost:3000 }}
            <a
              href=""
              class="btn btn-link"
              data-toggle="modal"
              data-target="#delete-course-modal"
              data-id="{{this._id}}"
            >Xóa Vĩnh Viễn</a>

            {{! #delete-course-modal huonwsg toiws id cuar modal }}
          </td>
        </tr>
      {{else}}
        <tr>
          <td colspan="5" class="text-center">
            Thùng rác trống
            <a href="/courses/create">Đăng khóa học ngay và luôn</a>
          </td>
        </tr>
      {{/each}}

    </tbody>
  </table>
</div>
{{! Confirm }}

<div id="delete-course-modal" class="modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Modal title</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Hành động này không thể khôi phục. Bạn có muốn xóa??</p>
      </div>
      <div class="modal-footer">
        <button
          id="btn-force-delete-course"
          type="button"
          class="btn btn-danger btn-force-delete"
        >Xóa Vĩnh Viễn</button>
        <button
          type="button"
          class="btn btn-primary"
          data-dismiss="modal"
        >Hủy</button>
      </div>
    </div>
  </div>
</div>

{{! DELETE form }}

{{! <form name="force-delete-course-form" method="POST"> }}
<form name="force-delete-course-form" method="POST">

</form>
{{! UPdate Form }}
<form name="restore-course-form" method="POST"></form>
<script src="/js/delete-course.js">

</script>